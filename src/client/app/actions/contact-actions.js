export const formName = (data) => ({
  type: 'FORM_NAME',
  data
});

export const formEmail = (data) => ({
  type: 'FORM_EMAIL',
  data
});

export const formMessage = (data) => ({
  type: 'FORM_MESSAGE',
  data
});
